# Production build (npm run build / npm run deploy) — GitHub Pages.
# The Client ID is read from the local, gitignored .env (it also loads here).
# This file only overrides the redirect URI to the deployed callback URL.
#
# Add this EXACT URI to your Spotify app's Redirect URIs in the dashboard:
#   https://ed-ward239.github.io/Verseflow/callback
REACT_APP_SPOTIFY_REDIRECT_URI=https://ed-ward239.github.io/Verseflow/callback
